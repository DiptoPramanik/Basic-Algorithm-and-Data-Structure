#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int *a,*b;
    ///can't do it
    a =3,b=4;
    ///can dot it
    a = b;
    cout<<a<<' '<<b<<endl;
    return 0;
}

